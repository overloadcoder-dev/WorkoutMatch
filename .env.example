# Optional public production origin used for canonical URLs and sitemap output.
PUBLIC_SITE_URL=https://workoutmatch.example

# Optional deployment path. Keep / for a root domain; use /WorkoutMatch for
# the configured GitHub Pages project site.
PUBLIC_BASE_PATH=/
